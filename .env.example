# Shared environment variables
VITE_API_BASE_URL=/

# Development server config
VITE_HOST=0.0.0.0
VITE_PORT=5173
FRONTEND_HOST_PORT=15173

# Local backend URL used by docker-compose.dev.yml
API_BASE_URL=http://host.docker.internal:18080
INVOICING_API_BASE_URL=http://host.docker.internal:8080

# Production host port for nginx container
FRONTEND_PROD_HOST_PORT=15080
